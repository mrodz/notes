HW Password: "boola"

```racket
#lang racket

(define (greet str) (display (string-append "Hello, " str "\n")))

(greet "Mateo")

(define (fizzbuzz int) (
  cond
    [(and (zero? (modulo int 3)) (zero? (modulo int 5))) "FizzBuzz"]
    [(zero? (modulo int 3)) "Fizz"]
    [(zero? (modulo int 5)) "Buzz"]
    [else number->string int]
))

(for ([i 31])
  (printf "~a: ~a\n" i (fizzbuzz i))
)
```

```racket
#lang racket

(define (collatz n)
  (if (zero? (modulo n 2)) (/ n 2)
    (+ 1 (* n 3))))

(define (c-series n)
  (print n)
  (newline)
  (if (eq? n 1) display "Done\n")
    (let ((next (collatz n)))
    (c-series next)
  )
)
```